import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!prisma) return NextResponse.json({ error: "Banco de dados não configurado." }, { status: 503 }); try { const body = await request.json(); const data: Record<string, unknown> = {}; for (const key of ["code", "type", "active"]) if (key in body) data[key] = key === "code" ? String(body[key]).trim().toUpperCase() : body[key]; for (const key of ["value", "minimumValue", "usageLimit"]) if (key in body) data[key] = body[key] === "" || body[key] == null ? null : Number(body[key]); for (const key of ["startsAt", "endsAt"]) if (key in body) data[key] = new Date(body[key]); return NextResponse.json(await prisma.coupon.update({ where: { id: (await params).id }, data })); } catch { return NextResponse.json({ error: "Não foi possível atualizar o cupom." }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!prisma) return NextResponse.json({ error: "Banco de dados não configurado." }, { status: 503 }); try { await prisma.coupon.delete({ where: { id: (await params).id } }); return NextResponse.json({ deleted: true }); } catch { return NextResponse.json({ error: "Não foi possível excluir o cupom." }, { status: 400 }); } }