import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!prisma) return NextResponse.json({ error: "Banco de dados não configurado." }, { status: 503 }); try { const body = await request.json(); const data: Record<string, unknown> = {}; for (const key of ["mainProductId", "offeredProductId", "title", "description", "image", "active"]) if (key in body) data[key] = body[key]; if ("specialPrice" in body) data.specialPrice = Number(body.specialPrice); return NextResponse.json(await prisma.upsellOffer.update({ where: { id: (await params).id }, data })); } catch { return NextResponse.json({ error: "Não foi possível atualizar a oferta." }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!prisma) return NextResponse.json({ error: "Banco de dados não configurado." }, { status: 503 }); try { await prisma.upsellOffer.delete({ where: { id: (await params).id } }); return NextResponse.json({ deleted: true }); } catch { return NextResponse.json({ error: "Não foi possível excluir a oferta." }, { status: 400 }); } }