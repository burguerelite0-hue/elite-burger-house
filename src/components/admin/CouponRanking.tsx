"use client";

import { Award, BarChart3, TicketPercent } from "lucide-react";
import { useEffect, useState } from "react";

type Ranking = { code: string; uses: number; revenue: number; discount: number };
export function CouponRanking() { const [data, setData] = useState<{ totalDiscount: number; totalOrders: number; ranking: Ranking[] } | null>(null); useEffect(() => { fetch("/api/admin/coupons/stats").then((response) => response.json()).then(setData).catch(() => undefined); }, []); const money = (value: number) => `R$ ${value.toFixed(2).replace(".", ",")}`; return <section className="rounded-2xl border border-[#e9e7ea] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-[#85828c]">Performance promocional</p><h3 className="mt-1 text-lg font-bold text-[#1d1b20]">Ranking de cupons</h3></div><TicketPercent className="text-[#e33b32]" size={22} /></div>{!data ? <div className="mt-6 h-24 animate-pulse rounded-xl bg-[#f4f2f4]" /> : <div className="mt-5 grid gap-5 lg:grid-cols-[1fr_1.2fr]"><div className="grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#fff7e4] p-4"><Award size={17} className="text-[#bd8421]" /><p className="mt-3 text-xs text-[#85828c]">Mais usado</p><p className="mt-1 text-lg font-black text-[#29262d]">{data.ranking[0]?.code ?? "-"}</p><p className="text-xs text-[#bd8421]">{data.ranking[0]?.uses ?? 0} usos</p></div><div className="rounded-xl bg-[#fff0ef] p-4"><BarChart3 size={17} className="text-[#e33b32]" /><p className="mt-3 text-xs text-[#85828c]">Descontos dados</p><p className="mt-1 text-lg font-black text-[#29262d]">{money(data.totalDiscount)}</p><p className="text-xs text-[#e33b32]">{data.totalOrders} pedidos</p></div></div><div className="space-y-3">{data.ranking.slice(0, 5).map((coupon, index) => <div key={coupon.code} className="flex items-center gap-3"><span className="w-5 font-display text-xl text-[#e33b32]">0{index + 1}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-2 text-xs"><strong>{coupon.code}</strong><span className="text-[#85828c]">{coupon.uses} usos</span></div><div className="mt-1 h-2 rounded-full bg-[#f0eef1]"><div className="h-2 rounded-full bg-[#e33b32]" style={{ width: `${Math.max(8, (coupon.uses / Math.max(data.ranking[0]?.uses ?? 1, 1)) * 100)}%` }} /></div></div><span className="text-xs font-bold text-[#2a8b67]">{money(coupon.revenue)}</span></div>)}</div></div>}</section>; }